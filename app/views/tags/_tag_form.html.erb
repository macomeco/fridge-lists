<%= form_with(model: tag, local: true)do |f| %>
    <div class="form-group">
        <%= f.text_field :name, class: 'form-control form-control-sm'%>
        <div class="text-right mt-3">
            <%= f.submit '追加' , class: 'btn btn-sm clr_sub_b clr_font_c clr_font_bd' %>
        </div>
    </div>
<%end%>

