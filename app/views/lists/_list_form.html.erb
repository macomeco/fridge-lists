<div class="card mt-2">
    <div class="card-body">
        <%= form_with(model: list, local: true)do |f| %>
        <div class="form-group">
            <div class="form-inline">
                <%= f.text_field :name, class: 'form-control'%>
                <%= f.submit '追加' , class: 'btn btn-success ml-2' %>
            </div>
        </div>
        <%end%>
     </div>
</div> 